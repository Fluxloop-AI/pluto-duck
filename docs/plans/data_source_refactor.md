좋은 인사이트입니다! 사용자 멘탈 모델 관점에서 기획을 정리해드리겠습니다.

---

## 🎯 Data Management UX 재설계 기획안

### 현재 문제점

| 현재 구조 | 문제 |
|-----------|------|
| Data Sources 모달 (Attach + Cache) | "소스"와 "캐시"가 같이 있어 혼란 |
| Assets → Imported Files 탭 | Files가 왜 Assets에 있는지 직관적이지 않음 |
| Raw/Work/Asset 3단계 | 내부 구현 개념이 UI에 노출됨 |

---

### 제안: 2-레이어 모델

```
┌─────────────────────────────────────────────────────────────────┐
│  [Connect Data] 버튼 (헤더바)                                    │
│  → 연결만 담당하는 가벼운 모달                                    │
└─────────────────────────────────────────────────────────────────┘
                              ↓ 연결 후
┌─────────────────────────────────────────────────────────────────┐
│  Assets → [Data Sources] 탭                                      │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ [Live Connections] [Cached Tables] [Imported Files]         ││
│  │     (실시간 쿼리)      (스냅샷)       (파일 데이터)           ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

---

### 레이어 1: Connect Data (연결 진입점)

**역할**: 외부 데이터 소스 연결만 담당

```
┌─────────────────────────────────────────────┐
│  Connect Data                               │
├─────────────────────────────────────────────┤
│                                             │
│  📁 Import File                             │
│     CSV, Parquet 파일 가져오기              │
│                                             │
│  🐘 PostgreSQL                              │
│     Connect to PostgreSQL database          │
│                                             │
│  📦 SQLite                                  │
│     Connect to SQLite file                  │
│                                             │
│  🦆 DuckDB                                  │
│     Connect to DuckDB file                  │
│                                             │
└─────────────────────────────────────────────┘
```

- 연결 성공 시 → Assets의 Data Sources 탭으로 자동 이동
- 복잡한 테이블 관리 UI 없음 (단순 연결만)

---

### 레이어 2: Assets → Data Sources 탭 (통합 관리)

**3가지 서브 카테고리:**

#### 1️⃣ Live Connections (실시간 연결)
- 현재의 Attached 소스들
- 실시간 쿼리 가능
- 상태: 🟢 Connected / 🔴 Disconnected

```
┌──────────────────────────────────────────────────────────┐
│ 🐘 my_postgres                              🟢 Connected │
│    Tables: users, orders, products (3)                   │
│    [Browse Tables] [Create Snapshot] [Disconnect]        │
└──────────────────────────────────────────────────────────┘
```

#### 2️⃣ Cached Tables (스냅샷)
- 특정 시점의 테이블 스냅샷
- Live Connection에서 파생됨 (관계 표시)
- TTL 또는 영구 보존 선택

```
┌──────────────────────────────────────────────────────────┐
│ 📸 users_snapshot_20260110                               │
│    From: my_postgres.users                               │
│    Created: 2026-01-10 14:30 | Rows: 1,234               │
│    [Preview] [Refresh] [Delete]                          │
└──────────────────────────────────────────────────────────┘
```

#### 3️⃣ Imported Files (파일 데이터)
- CSV, Parquet 직접 임포트
- 증분 추가 지원 (Append)
- Live Stack 개념 (아래 설명)

```
┌──────────────────────────────────────────────────────────┐
│ 📄 sales_data.csv                                        │
│    Table: sales_2024 | Rows: 50,000                      │
│    [Preview] [Append Data] [Replace] [Delete]            │
│                                                          │
│    📊 Import History:                                    │
│    └─ v3: +5,000 rows (2026-01-10)                      │
│    └─ v2: +10,000 rows (2026-01-05)                     │
│    └─ v1: 35,000 rows (2026-01-01)                      │
└──────────────────────────────────────────────────────────┘
```

---

### 🔥 Files의 Live Stack 개념

파일이지만 "살아있는" 데이터로 활용:

```
┌─────────────────────────────────────────────────────────────┐
│  Imported File: monthly_sales                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  📈 Stack Mode: Append (Incremental)                        │
│                                                             │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ sales_jan.csv    → 10,000 rows   [2026-01]          │   │
│  │ sales_feb.csv    → 12,000 rows   [2026-02]          │   │
│  │ sales_mar.csv    → 11,500 rows   [2026-03]          │   │
│  │ ─────────────────────────────────────────────       │   │
│  │ Total: 33,500 rows                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  [+ Add Next Month] [Merge All] [Query as One]             │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**지원 모드:**
- **Replace**: 기존 데이터 교체
- **Append**: 기존 데이터에 추가 (증분)
- **Merge**: 키 기반 UPSERT (중복 제거)

---

### 관계 시각화 (선택적)

```
Live Connection          Cached Tables           Imported Files
     │                        │                        │
     │    ┌───────────────────┤                        │
     │    │                   │                        │
  ┌──▼────▼──┐         ┌──────▼──────┐         ┌──────▼──────┐
  │ postgres │──────→│ users_cache  │         │ sales.csv   │
  │          │        └──────────────┘         │  (stacked)  │
  │          │──────→│ orders_cache │         └─────────────┘
  └──────────┘        └──────────────┘
```

---

### 구현 우선순위

| Phase | 작업 | 복잡도 |
|-------|------|--------|
| **P1** | Connect Data 버튼 이름 변경 + 모달 단순화 | 🟢 낮음 |
| **P1** | Assets에 "Data Sources" 탭 추가 | 🟢 낮음 |
| **P1** | 3가지 카테고리 (Live/Cached/Files) UI | 🟡 중간 |
| **P2** | Live → Cached 생성 연동 | 🟡 중간 |
| **P3** | Files의 Append/Merge 기능 | 🟡 중간 |
| **P3** | Import History 추적 | 🔴 높음 |

---

### 사용자 멘탈 모델 정리

| 사용자가 이해할 개념 | 내부 구현 |
|---------------------|----------|
| **Live Connection** = 실시간 연결 | Raw Zone (ATTACH) |
| **Cached Table** = 스냅샷 | Work/Asset Zone |
| **Imported File** = 내 파일 | Asset Zone |

---

이 방향으로 진행할까요? 아니면 특정 부분을 조정하고 싶으신가요?